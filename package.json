{
  "name": "Notely",
  "private": true,
  "version": "0.1.0",
  "description": "An AngularJS 1.4 note-taking app.",
  "repository": "git@github.com/getfretless/notely-seed.git",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "start": "http-server app -a localhost -p 8000 -c-1",
    "prestart": "npm install",
    "postinstall": "bower install",
    "preupdate-webdriver": "npm install"
  }
}
